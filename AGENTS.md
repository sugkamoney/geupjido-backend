# Backend project guidance

## Project scope

이 저장소는 수도권 아파트 급지 서비스의 백엔드 API를 관리한다.

주요 범위는 다음과 같다.

- 지역·권역·아파트 단지 데이터 관리
- 위치 및 공간 정보 처리
- REST API 제공
- 데이터 수집과 가공을 위한 백엔드 작업

현재 기술 기준은 Java 21, Spring Boot, Gradle, PostgreSQL·PostGIS 및 Redis다.
새로운 라이브러리나 인프라 구성요소를 추가하기 전에는 목적과 영향을 먼저 설명한다.

## Working agreements

- 모든 설명과 작업 안내는 한국어로 작성한다.
- 작업을 시작하기 전에 GitHub Issue를 생성하고 작업 범위와 완료 조건을 정한다.
- 새로운 작업 브랜치는 최신 `develop`에서 생성한다.
- 브랜치 이름은 `{종류}/{이슈번호}-{작업명}` 형식을 사용한다.
  예: `feature/4-flyway-location-schema`, `docs/7-codex-project-guidelines`
- 작업 브랜치는 `develop`으로 Pull Request를 생성한다.
- 완료된 `develop`은 Pull Request를 통해 `main`으로 병합한다.
- `main`과 `develop`에 직접 커밋하거나 강제 푸시하지 않는다.
- 한 이슈에서는 합의된 작업 범위만 변경한다.

## Collaboration with Codex

- 사용자가 애플리케이션 코드와 비즈니스 로직을 직접 작성한다.
- Codex는 작업 목적, 파일 위치, 작성할 코드와 검증 방법을 한 단계씩 안내한다.
- 한 번에 과도한 작업을 제시하지 않고 현재 단계가 완료된 뒤 다음 단계로 진행한다.
- 설정 파일이나 반복적인 기반 코드는 사용자가 요청한 경우 Codex가 작성할 수 있다.
- Codex는 새로운 의존성을 추가하기 전에 필요성과 영향을 설명한다.
- Codex는 임의로 브랜치를 생성하거나 커밋, 푸시, Pull Request 생성 또는 병합을 하지 않는다.
- 사용자가 코드 작성을 마치면 Codex는 변경 내용과 실행 결과를 확인한다.
- 기존 코드나 설정을 변경하기 전에 현재 구조와 변경 영향을 먼저 확인한다.

## Documentation

Notion과 저장소 문서는 서로 다른 목적으로 사용한다.

- Notion: 현재 프로젝트 단계, 일일 작업 내용, 완료 여부 및 다음 작업
- `docs/architecture/`: 패키지 구조, 데이터베이스, API, 인증, 테스트 및 배포 설계
- `docs/learning/backend/q-and-a/`: 실제 작업 중 학습한 백엔드 개념과 프로젝트 적용 방법
- `docs/history/`: 중요한 기술 결정, 검토한 대안, 선택 이유 및 트레이드오프

의미 있는 구조나 동작, 기술 결정이 변경되면 관련 문서도 함께 갱신한다. 단순 포맷 변경,
파일명 변경, 명백한 오타 수정과 같은 기계적 작업은 별도 기록으로 남기지 않는다.

학습 기록은 일반 개념뿐 아니라 이 프로젝트에 어떻게 적용되는지도 설명한다. 프로젝트와
관련 없는 일반 자료를 그대로 복사하지 않는다.

비밀번호, 접근 토큰, 개인정보, 실제 환경 변수 값 또는 민감한 배포 정보는 문서와 코드에
기록하지 않는다.

## Backend development rules

- 도메인 중심 패키지 구조를 유지한다.
- Controller는 요청 검증과 응답 변환을 담당하고 비즈니스 로직을 포함하지 않는다.
- Service는 유스케이스와 트랜잭션 경계를 담당한다.
- Repository는 영속성 접근을 담당한다.
- JPA 연관관계는 필요한 경우에만 정의하고 지연 로딩을 기본으로 한다.
- API 요청과 응답에는 엔티티를 직접 노출하지 않고 별도의 DTO를 사용한다.
- 예외 응답 형식은 프로젝트 공통 규칙을 따른다.
- 환경별 설정은 `application-{profile}.yml`로 분리한다.
- 비밀값과 실제 환경 변수 값은 저장소에 커밋하지 않는다.

## Database rules

- 데이터베이스 스키마는 Flyway로 관리한다.
- 이미 적용된 Flyway 마이그레이션 파일은 수정하지 않는다.
- 스키마 변경은 새로운 버전의 마이그레이션 파일로 추가한다.
- Hibernate가 스키마를 자동 생성하거나 변경하지 않도록 한다.
- 공간 컬럼은 PostgreSQL·PostGIS와 Hibernate Spatial의 타입 호환성을 확인한다.
- 운영 데이터베이스 동작을 H2 결과만으로 판단하지 않는다.

## Commit convention

커밋 메시지는 다음 형식을 사용한다.

```text
[#이슈번호] 타입: 한글 제목

- 간결한 작업 내용
```

이슈가 없는 작업은 `[#이슈번호]`를 생략한다. 타입은 `feat`, `fix`, `docs`, `chore`,
`refactor`, `test`, `perf`, `ci`, `build`, `style`, `revert` 중에서 선택한다.

제목은 간결한 한글로 작성하고, 본문은 실제 변경 사항만 짧게 정리한다. 관련 이슈를
자동으로 종료해야 할 때는 필요에 따라 `Fixes #이슈번호`를 마지막 줄에 추가한다.

## Verification

- 변경 범위에 맞는 테스트를 먼저 실행한다.
- 최종적으로 `./gradlew clean build`가 성공하는지 확인한다.
- 데이터베이스 매핑 변경은 로컬 PostgreSQL·PostGIS 환경에서도 검증한다.
- 완료했다고 판단하기 전에 명령의 실제 결과를 확인한다.
- 검증에 실패하면 실패 원인과 아직 확인하지 못한 항목을 명확하게 알린다.
