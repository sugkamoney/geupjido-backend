spring:
  application:
    name: geupjido-backend
  profiles:
    default: local
  jpa:
    open-in-view: false

server:
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
